'use client';

export default function PropertyViewer() {
  return (
    <div style={{ marginTop: 24, height: 320, border: '1px dashed #999', borderRadius: 12, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <div>
        <h3>3D Viewer Placeholder</h3>
        <p>Export a GLB from Blender, then wire it here with Three.js.</p>
      </div>
    </div>
  );
}
